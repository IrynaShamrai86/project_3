import '../../dist/css/logo.css';
import logo from '../../dist/img/logo.webp';

const Logo = () => {
  return (
    <div className="logo">
      <img src={ logo } />
    </div>
  )
}

export default Logo;
